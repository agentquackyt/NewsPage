{
  "name": "newspage",
  "private": true,
  "scripts": {
    "start": "bun cli.ts",
    "serve": "bun cli.ts serve",
    "generate": "bun cli.ts generate ./dist",
    "config": "bun cli.ts config",
    "bundle": "bun build --compile --minify cli.ts --outfile newspage",
    "flavortown": "bun build --compile --minify flavortown.ts --outfile Output/flavortown"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/marked": "^6.0.0",
    "@types/node": "^25.3.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "gray-matter": "^4.0.3",
    "marked": "^17.0.3"
  }
}